import reducer from './reducers';
import * as catalogOperation from './operations';
import * as stateSelectors from './state-selectors';

export {
    catalogOperation,
    stateSelectors
};

export default reducer;
